<%
    const {
        name,
        dependencies,
        version,
        type,
        module,
        files
    } = data ;
%>
{
    "name": "<%- name %>",
    "version":"<%- version %>",
    "private": true,
    "files": <%- JSON.stringify(files , null , 2) %>,
    "dependencies": <%- JSON.stringify(dependencies , null , 2) %>
}