<%
    const {
        name,
        dependencies,
        version,
        type
    } = data ;
%>
{
    "name": "<%- name %>",
    "version":"<%- version %>",
    "private": true,
    "files":[
        "index.js"
    ],
    "dependencies": <%- JSON.stringify(dependencies , null , 2) %>
}