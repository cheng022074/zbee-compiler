<%
    const {
        name,
        dependencies,
        version,
        type,
        module
    } = data ;
%>
{
    "name": "<%- name %>",
    "version":"<%- version %>",
    "private": true,
    "dependencies": <%- JSON.stringify(dependencies , null , 2) %>
}