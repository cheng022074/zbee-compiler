<%
    const {
        name,
        dependencies,
        version,
        type,
        module,
        files
    } = data ;
%>
{
    "name": "<%- name %>",
    "version":"<%- version %>",
    "author": "nero chen",
    "license": "ISC",
    "files": <%- JSON.stringify(files , null , 2) %>,
    "dependencies": <%- JSON.stringify(dependencies , null , 2) %>
}